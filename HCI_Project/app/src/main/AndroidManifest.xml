<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="day01.huy.hci_project">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeActivity"></activity>
        <activity android:name=".FridgeActivity" />
        <activity android:name=".IngredientAmountActivity" />
        <activity android:name=".RecipeActivity" />
        <activity android:name=".GetPermissionActivity" />
        <activity android:name=".DetailActivity" />
        <activity android:name=".DishesTypeActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".SearchResultActivity" />
        <activity android:name=".NavigationActivity" />
        <activity android:name=".PickIngredientActivity" />
        <activity android:name=".PostRecipeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>